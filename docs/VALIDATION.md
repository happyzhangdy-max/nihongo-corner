# 2026-09-06 主题皮肤验收

- 首页新增夜色、和纸、樱花、深海四款皮肤，首次绘制前恢复当前浏览器的选择；四款实际背景颜色及单一选中状态已验证。
- 四款主题在 320、390、1280 像素宽度下均无首页横向溢出，按钮高度至少 44 像素；Enter / Space 键切换、刷新后保留均通过。
- 和纸主题走查首页、词汇、语法、自动学习、场景、设置、识图、学习计划、复习、生词本和模拟题 11 个页面，修正历史硬编码白字、复习入口与辅助文字。基础主/次/三级文字在三种主要表面上的最低对比为 5.18:1，主要按钮最低为 6.61:1；这不是对所有动态内容和装饰图形的完整无障碍认证。
- 非法偏好值、原型属性名及主题存储读取/写入被拒时可以正常打开和切换，保存失败会提示。主题只写 nihongo_theme_v1；既有词汇收藏与复习示例值逐字保持。
- 60 项回归、28 个脚本检查及页面运行检查通过。未增加第三方依赖；词汇、场景对话、学习队列及识图逻辑未改动。

# 2026-09-05 修复验证

最新场景学习更新：见 [场景句子与参考来源](SCENE_SENTENCES.md)。当前 60 项测试、27 个脚本检查、全库结构检查及下述场景验收通过。

## 场景对话扩充验收

- 24 场景各由 6 组扩充为 12 组，现有 288 组双人短对话、576 句。本次新增 144 组，原有 144 组内容及 ID 逐项比较保持不变。结构、稳定 ID、任务唯一性、假名完整性、重复句检查通过。
- 新增内容先由三位编写者逐组自审，再交叉复核全部 144 组；修正了任务标题超出实际表达、机场出发时刻译法及病史描述预设因果的问题。
- 隔离浏览器在 320、390、1280 像素宽度下逐一走查全部 24 场景：每场景 12 组卡片、13 个任务选项（含全部任务）、新任务筛选及句数均正确，无横向溢出与未捕获 JavaScript 异常。桌面和手机统一使用紧凑任务选择框。
- 新增餐厅任务的双人连播、暂停/继续、暂停时切句、停止和单句精确重播通过；全场景队列为 24 句，单任务只含对应的两句。搜索新增对方回应可以找到相应场景。
- 浏览器临时加入第 13 组的混合数量样本后，场景和目录统计同步变化；样本只用于验证，没有写入正式数据。原有词汇收藏及复习示例状态保持原值。
- 上一轮已验证旧 legacy:all 加选场景的迁移、显示偏好、进出焦点与原生语音结束回调。本轮相关自动回归继续通过，没有重新逐句听校设备合成语音。
- 本轮没有重新调用识图收费接口；原识图、搜索与复习回归仍通过。手机硬件相机、iOS Safari 与 APK 的原有验证边界继续适用。

上一轮主题词库分类更新：见 [主题与场景词库](SCENE_CATALOG.md)。该轮为 53 项测试与 24 个脚本检查。

后续的词汇、语法与 UI 优化见 [内容校订与学习界面优化](CONTENT_REVIEW_2026-09-05.md)。以下保留第一轮识图与学习流程修复的验证记录；该轮完整测试数为 46 项。

## 已复现并修复

| 问题 | 修复 |
| --- | --- |
| 拍照识图返回 404：旧模型无可用端点 | 改用现有代理已验证可用的 Gemini 2.5 Flash |
| 图片粘贴存在两个监听器 | 统一为一次读取、一次请求 |
| 图片解码失败没有退出路径，竖图没有限制长边 | 解码错误/超时退出，统一 JPEG，长边最多 1600 像素 |
| 15 秒请求超时、失败后丢图 | 识图等待上限 90 秒，保留图片并允许重试 |
| 清除或换图后旧请求仍回填 | 中止请求并校验请求代次 |
| 历史未保存解析，旧页面补丁将原文覆盖到解析区 | 保存完整解析，删除冲突补丁，兼容旧历史 |
| 存储不足导致成功结果被清掉；历史清空后可能复活 | 存储故障与识别成功分开处理，同步清空内存历史 |
| 搜索在 WebView 兼容代码中重复绑定；旧请求覆盖新结果 | 使用可替换的事件绑定，中止旧请求并忽略过期结果 |
| 搜索缓存损坏导致搜索中断 | 忽略无效缓存并重新请求 |
| SM-2 读取了错误的字段，复习间隔一直是 1 天 | 读取 interval/repetitions/ef，兼容原缩写字段 |
| touchmove 误调用拖动结束回调 | 正确区分移动和结束事件，并移除监听器 |
| 两处单词收藏按钮写入字面量 +v.id+ | 写入实际词汇 ID |

## 自动化检查

- node check.js：19 个脚本通过语法检查，页面引用的脚本文件均存在；失败时返回非零退出码。
- node --test：35 项测试通过，无第三方测试依赖。
- 测试涵盖 HTTP/非 JSON/空内容/截断/超时/取消、换图竞态、重复请求、失败重试、历史恢复/清空/损坏/存储不足、搜索并发和缓存、复习进阶及触屏事件。
- git diff --check：通过。

## 浏览器验证

使用隔离 Chromium 浏览器数据，不读取或更改日常浏览器的学习数据。

- 真实接口：合成图片中的“今日は図書館で日本語を勉強します。本を三冊借りました。”被提取，并返回中文翻译、词汇和语法讲解（HTTP 200）。
- 真实搜索：“明日の会議に少し遅れます”返回中文含义与语法解释。
- 模拟异常：损坏图片能退出加载；503 响应保留图片，点击重试可成功；相机文件输入可接收图片；粘贴只发送一次请求。
- 600×3000 竖图发送前转换为 320×1600 JPEG。
- 原文/译文切换、历史回看恢复完整解析、手机和桌面宽度下的显示通过检查。
- 首页、词库、语法、复习、生词本、错题本、播放、游戏入口、模拟题、学习计划、设置均可打开，未出现未捕获 JavaScript 异常。
- 单词收藏后能在生词本找到相同词汇；10 题模拟测验能答题、显示反馈并到达结果页；播放可开始、暂停、切词、停止。

## 验证边界与后续

- 未连接手机实机：没有验证硬件相机权限、iOS Safari 或原生 Capacitor APK。文件输入测试验证的是网页接收图片后的流程。
- 未自动审校所有词库/题库内容，也没有穷举游戏关卡或所有学习计划组合。
- 仍依赖外部代理、网络和模型服务；服务故障会显示可重试错误。
- 本次只提取识图和 AI 请求职责；压缩在 inline.js 中的其他职责保留，后续拆分需独立回归。
- N1新词库仍在独立草稿 PR #3，本次修复不包含该功能。
